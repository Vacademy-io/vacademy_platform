package vacademy.io.admin_core_service.features.catalogue_blog.enums;

/** Lifecycle of a catalogue blog post. Only PUBLISHED rows are served publicly. */
public enum BlogPostStatus {
    DRAFT,
    PUBLISHED,
    ARCHIVED
}
