package vacademy.io.admin_core_service.features.counselor_pool.enums;

/**
 * Status applied to pool members, shifts, and shift members.
 * Distinct from the user account status (which lives on the users table).
 */
public enum PoolStatus {
    ACTIVE,
    INACTIVE
}
