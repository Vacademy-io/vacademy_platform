package vacademy.io.admin_core_service.features.doubts.enums;

/** Who performed a doubt activity: a person, a configured routing rule, or the system itself. */
public enum DoubtActivityActorTypeEnum {
    USER, RULE, SYSTEM
}
