package vacademy.io.admin_core_service.features.onboarding.enums;

/** v1 ships FORM only. Extend this (and add a matching OnboardingStepTypeHandler) for new step types. */
public enum OnboardingStepTypeEnum {
    FORM
}
