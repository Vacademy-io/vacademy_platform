fastapi==0.115.0
uvicorn[standard]==0.30.6
boto3==1.34.0
moviepy>=2.0.0
playwright==1.49.1
beautifulsoup4==4.12.3
httpx==0.27.2
pydantic==2.9.2

# -- Video indexing pipeline (Step 2) --
faster-whisper>=1.0.0
mediapipe==0.10.14
librosa>=0.10.0
soundfile>=0.12.1
scenedetect>=0.6.1
rapidocr-onnxruntime>=1.3.0
opencv-python-headless>=4.8.0
# Cap below 2.0: mediapipe==0.10.14 and paddlepaddle 2.6.x ship C-extensions
# compiled against the numpy 1.x ABI (paddle gained numpy-2 support only in
# 3.0). With an unbounded pin the image pulled numpy 2.x and every job crashed
# at import time with "numpy.core.multiarray failed to import". Keep <2 until
# both mediapipe and paddlepaddle are bumped to numpy-2-compatible releases.
numpy>=1.24.0,<2
scipy>=1.11.0
openai>=1.0.0
protobuf>=3.20,<5

# -- Image indexing pipeline --
Pillow>=10.0.0
rembg>=2.0.50

# -- PDF copy-check OCR (handwritten answer sheets) --
# Pin tightly: 2.8 changed PaddleOCR's call signature (paddle.predict(...))
# and result shape; the layout_ocr parser expects the 2.7 contract. Bump
# version + parser together when we want a newer release.
paddleocr>=2.7.0,<2.8.0
paddlepaddle>=2.6.0,<2.7.0
pymupdf>=1.24.0
