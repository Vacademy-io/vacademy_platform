fastapi==0.115.0
python-multipart>=0.0.9
uvicorn[standard]==0.30.6
SQLAlchemy==2.0.35
psycopg[binary]==3.2.12
pydantic-settings==2.6.1
python-dotenv==1.0.1
httpx==0.27.2
boto3==1.34.0
sse-starlette==1.6.5
edge-tts
moviepy==1.0.3
playwright==1.49.1
beautifulsoup4==4.12.3
google-cloud-texttospeech>=2.17.0
faster-whisper
python-jose[cryptography]==3.3.0
nltk
mutagen
rembg
# Phase 2d (PiP alpha-matte cutout) — mediapipe SelfieSegmentation produces
# per-frame alpha mattes for reel speaker silhouettes; opencv reads source
# video frames + writes RGBA streams to ffmpeg. Pinned to match the versions
# already validated in render_worker/requirements.txt so we don't fork
# matting behavior across services.
mediapipe==0.10.14
opencv-python-headless>=4.8.0
# Cap below 2.0 — mediapipe==0.10.14 is built against the numpy 1.x ABI, so an
# unbounded pin lets pip pull numpy 2.x and the C-extension import fails with
# "numpy.core.multiarray failed to import". Matches render_worker/requirements.txt.
numpy>=1.24.0,<2
pgvector>=0.3.0
websockets>=12.0
pdfplumber>=0.10.0
# PyMuPDF — used by copy_check/mathpix_fallback.py to rasterize PDF pages
# at 200 DPI (matching render_worker) and crop low-confidence math lines
# for selective Mathpix re-OCR. Without this, mathpix.enrich_layout_for_math
# crashes with "No module named 'fitz'" right after LAYOUT_OCR_DONE.
pymupdf>=1.24.0
