package vacademy.io.community_service.feature.onboarding.enums;

/** Pipeline status of an onboarding submission, managed from the super-admin Demo tab. */
public enum SubmissionStatus {
    NEW,
    VIEWED,
    CONTACTED,
    WON,
    LOST
}
