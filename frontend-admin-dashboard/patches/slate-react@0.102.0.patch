diff --git a/dist/index.es.js b/dist/index.es.js
index 91843e3bb0a915db87db4e675f1326d956b8968a..cc5f786edb7e3a647080c95d999a03b758993499 100644
--- a/dist/index.es.js
+++ b/dist/index.es.js
@@ -482,6 +482,8 @@ var ReactEditor = {
     return range;
   },
   findKey: (editor, node) => {
+    // PATCH(vacademy): tolerate a null node.
+    if (node == null) return new Key();
     var key = NODE_TO_KEY.get(node);
     if (!key) {
       key = new Key();
@@ -490,6 +492,8 @@ var ReactEditor = {
     return key;
   },
   findPath: (editor, node) => {
+    // PATCH(vacademy): unresolvable node maps to document root [].
+    if (node == null) return [];
     var path = [];
     var child = node;
     while (true) {
@@ -510,7 +514,7 @@ var ReactEditor = {
     }
     throw new Error("Unable to find the path for Slate node: ".concat(Scrubber.stringify(node)));
   },
-  focus: function focus(editor) {
+  focus: function focus(editor) { try {
     var options = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {
       retries: 5
     };
@@ -554,7 +558,7 @@ var ReactEditor = {
         preventScroll: true
       });
     }
-  },
+   } catch (_e) { /* PATCH(vacademy): editor DOM node/selection can be transiently unresolvable (e.g. right after a paste, before the block DOM paints) so toDOMNode/toDOMRange/Editor.start throw; focus is best-effort and must NEVER crash the slide view. */ } },
   getWindow: editor => {
     var window = EDITOR_TO_WINDOW.get(editor);
     if (!window) {
@@ -713,7 +717,13 @@ var ReactEditor = {
     }
     var node = domEl ? ELEMENT_TO_NODE.get(domEl) : null;
     if (!node) {
-      throw new Error("Cannot resolve a Slate node from DOM node: ".concat(domEl));
+      // PATCH(vacademy): return undefined instead of throwing so a DOM node that lives
+      // inside the editor but is NOT part of the Slate document (e.g. our nested
+      // contentEditable rich-text field, or the <ol>/<li>/<span> nodes that
+      // execCommand creates inside it) never crashes the global selectionchange /
+      // click / blur / drag handlers. Every caller already treats a missing node
+      // as "not a Slate node here".
+      return undefined;
     }
     return node;
   },
@@ -837,6 +847,13 @@ var ReactEditor = {
     // the select event fires twice, once for the old editor's `element`
     // first, and then afterwards for the correct `element`. (2017/03/03)
     var slateNode = ReactEditor.toSlateNode(editor, textNode);
+    if (slateNode == null) {
+      // PATCH(vacademy): text node couldn't be resolved; return null when suppressing.
+      if (suppressThrow) {
+        return null;
+      }
+      throw new Error("Cannot resolve a Slate point from DOM point: ".concat(domPoint));
+    }
     var path = ReactEditor.findPath(editor, slateNode);
     return {
       path,
diff --git a/dist/index.js b/dist/index.js
index e893f6ae3749c5e492866056fd84e3ab2f5926f1..4455520d67ec5ee5d20acc8402f3fc9e6ec9eace 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -695,6 +695,9 @@ var ReactEditor = {
     return range;
   },
   findKey: function findKey(editor, node) {
+    // PATCH(vacademy): tolerate a null node (toSlateNode may now return undefined) —
+    // WeakMap.set(undefined) would otherwise throw.
+    if (node == null) return new Key();
     var key = NODE_TO_KEY.get(node);
     if (!key) {
       key = new Key();
@@ -703,6 +706,9 @@ var ReactEditor = {
     return key;
   },
   findPath: function findPath(editor, node) {
+    // PATCH(vacademy): an unresolvable node maps to the document root [] instead of
+    // throwing "Unable to find the path".
+    if (node == null) return [];
     var path = [];
     var child = node;
     while (true) {
@@ -723,7 +729,7 @@ var ReactEditor = {
     }
     throw new Error("Unable to find the path for Slate node: ".concat(slate.Scrubber.stringify(node)));
   },
-  focus: function focus(editor) {
+  focus: function focus(editor) { try {
     var options = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {
       retries: 5
     };
@@ -767,7 +773,7 @@ var ReactEditor = {
         preventScroll: true
       });
     }
-  },
+   } catch (_e) { /* PATCH(vacademy): editor DOM node/selection can be transiently unresolvable (e.g. right after a paste, before the block DOM paints) so toDOMNode/toDOMRange/Editor.start throw; focus is best-effort and must NEVER crash the slide view. */ } },
   getWindow: function getWindow(editor) {
     var window = EDITOR_TO_WINDOW.get(editor);
     if (!window) {
@@ -943,7 +949,13 @@ var ReactEditor = {
     }
     var node = domEl ? ELEMENT_TO_NODE.get(domEl) : null;
     if (!node) {
-      throw new Error("Cannot resolve a Slate node from DOM node: ".concat(domEl));
+      // PATCH(vacademy): return undefined instead of throwing so a DOM node that lives
+      // inside the editor but is NOT part of the Slate document (e.g. our nested
+      // contentEditable rich-text field, or the <ol>/<li>/<span> nodes that
+      // execCommand creates inside it) never crashes the global selectionchange /
+      // click / blur / drag handlers. Every caller already treats a missing node
+      // as "not a Slate node here".
+      return undefined;
     }
     return node;
   },
@@ -1067,6 +1079,14 @@ var ReactEditor = {
     // the select event fires twice, once for the old editor's `element`
     // first, and then afterwards for the correct `element`. (2017/03/03)
     var slateNode = ReactEditor.toSlateNode(editor, textNode);
+    if (slateNode == null) {
+      // PATCH(vacademy): text node couldn't be resolved; return null when suppressing
+      // instead of producing a bogus root-level point.
+      if (suppressThrow) {
+        return null;
+      }
+      throw new Error("Cannot resolve a Slate point from DOM point: ".concat(domPoint));
+    }
     var path = ReactEditor.findPath(editor, slateNode);
     return {
       path: path,
