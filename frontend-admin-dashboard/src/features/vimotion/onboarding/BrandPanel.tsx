import { OutputShowcase } from '../auth/OutputShowcase';

export function BrandPanel() {
    return (
        <OutputShowcase tagline="AI video, on brand, in minutes — see what your studio will ship." />
    );
}
