/**
 * Thin compatibility shim — the real implementation is the shared
 * catalogue style engine (see catalogue-style-engine.ts, byte-synced with
 * the admin copy via scripts/check-style-engine-sync.mjs).
 */
export * from './catalogue-style-engine';
