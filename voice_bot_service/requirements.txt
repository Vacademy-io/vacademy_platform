# 0.0.95 verified against every module path this service imports (review 2026-07-01):
# services.sarvam.stt.SarvamSTTService (first shipped in 0.0.93), aggregators
# llm_context + llm_response_universal, transports.network.fastapi_websocket,
# runner.utils.parse_telephony_websocket, serializers.plivo. Bumping the pin
# requires re-verifying those paths — they have moved between pipecat versions
# (0.0.79 lacks Sarvam STT entirely; 1.x moved the transports package).
# The `sarvam` extra is REQUIRED: pipecat's sarvam/stt.py imports the `sarvamai`
# SDK at module load and the service dies at boot without it (verified in prod logs).
pipecat-ai[silero,websocket,sarvam]==0.0.95
aiohttp>=3.9
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
python-dotenv>=1.0
